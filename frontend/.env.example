# Backend API'nin taban URL'i. Prod'da reverse proxy üzerinden aynı domain'in
# /api/v1 yoluna yönlendiriliyorsa (bkz. Caddyfile.example) bu değeri
# https://<domain>/api/v1 olarak ayarlayın.
VITE_API_URL=http://localhost:8000/api/v1
